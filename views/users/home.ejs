<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Accueil</title>
    <link rel="stylesheet" href="/styles/home.css" />
  </head>
  <body>
    <%- include("../partials/navbar.ejs", { role }) %>

    <div class="container">
      <header>
        <%if (isFilteredResults){%>
        <h1>Articles correspondants à votre recherche :</h1>
        <%} else {%>
        <h1>Articles en vente sur le site :</h1>
        <%}%>
      </header>

      <div class="item-list">
        <% items.forEach(item => { %>
        <a href="/item/<%= item._id %>" class="item-card">
          <div class="item-details">
            <h2><%= item.name %></h2>
            <p><strong>Prix:</strong> <%= item.price %> €</p>
            <p><strong>Description:</strong> <%= item.description %></p>
          </div>
        </a>
        <% }); %>
      </div>
    </div>
  </body>
</html>
